"""Infra layer: adapters for vector store, reranker, parser, embedding,
storage, worker/task runtime, repository adapters, LLM providers, registries.

`infra/` may depend on `core/` contracts, but never on `modules/`.
"""
