"""Business and platform-admin modules.

Each module lives in `modules/<name>/` with a `module.py` exposing a class
that satisfies `core.modules.Module`. Step A only creates the package; Steps
B-D move the existing platform-admin tools and the demo corpus here, and add
the first business module (`modules/ecommerce/`).
"""
